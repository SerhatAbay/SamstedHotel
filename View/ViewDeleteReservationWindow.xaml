<Window x:Class="SamstedHotel.View.ViewDeleteReservationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SamstedHotel.View"
        mc:Ignorable="d"
        Title="Se/Slet Reservationer" Height="450" Width="800">
    <Grid>
        <!-- ListView for reservations -->
        <DataGrid ItemsSource="{Binding Reservations}" SelectedItem="{Binding SelectedReservation}"
                  AutoGenerateColumns="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                  Margin="10,10,10,50"/>

        <!-- Delete button -->
        <Button Content="Slet Reservation" Command="{Binding DeleteReservationCommand}" 
                HorizontalAlignment="Center" VerticalAlignment="Bottom" Width="150" Margin="10"/>
    </Grid>
</Window>

